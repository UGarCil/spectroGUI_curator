from setuptools import setup, find_packages

'''
Using pygame as main framework, read and traverse two paths, containing the images and the annotations
received from students, in .png and .json formats, offering a simple GUI to traverse the annotaitons.
'''
from setuptools import setup, find_packages

setup(
    name="guicurator",
    version="0.0.1",
    packages=find_packages(),
)
